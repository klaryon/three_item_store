import React from "react"
// import { } from "./styles"

const CartLine = ({item}) => {
    return(
        <div>
            hola
            {item.item.count}
        </div>
    )
}

export default CartLine;